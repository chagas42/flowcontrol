{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default capabilities for the FlowControl window",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
